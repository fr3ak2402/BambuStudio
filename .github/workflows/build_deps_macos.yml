name: Build macOS deps

on:
  workflow_call:

jobs:
  build_deps:
    name: Build deps
    runs-on: macos-latest
    needs: check-for-updates
    steps:
      - name: Checkout
        uses: actions/checkout@v4